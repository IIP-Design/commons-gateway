// ////////////////////////////////////////////////////////////////////////////
// Types
// ////////////////////////////////////////////////////////////////////////////
export type TUserRole = 'super admin' | 'admin' | 'guest admin' | 'guest';

// ////////////////////////////////////////////////////////////////////////////
// Interfaces
// ////////////////////////////////////////////////////////////////////////////
export interface IUserEntry {
  email: string;
  expiration: string;
  familyName: string;
  name: string
  givenName: string;
  role: TUserRole;
  team: string;
}

export interface ITeam {
  active: boolean;
  id: string;
  name: string;
}

export interface IUiData {
  name: string;
  active: boolean;
}

// ////////////////////////////////////////////////////////////////////////////
// Generics
// ////////////////////////////////////////////////////////////////////////////
export type WithUiData<T> = T & IUiData;
