Resources:
  SQS2FA:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: content-gateway-${opt:stage}-email-2fa

  SQSAprimoUpload:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: content-gateway-${opt:stage}-aprimo-upload
