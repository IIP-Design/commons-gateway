Resources:
  SQSSend2FA:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: content-gateway-${opt:stage}-send-2fa

  SQSAprimoUpload:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: content-gateway-${opt:stage}-aprimo-upload
